<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shibushi">
    <!-- To get pictures from camera for new posts -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="com.android.camera.action.CROP" />
            <data android:scheme="content"
                android:mimeType="image/*"/>
        </intent>
    </queries> <!-- To use device camera -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shibushi">

        <activity
            android:name=".ViewWardrobeActivity"
            android:exported="true" />
        <activity
            android:name=".TagIt"
            android:exported="true" />
        <activity
            android:name=".Login.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="true"
            android:screenOrientation="portrait" />
        <activity
            android:name=".firestoreUpload"
            />
        <activity
            android:name=".Login.Register"
            android:exported="true" />
        <activity android:name=".ViewOutfitsActivity" />

        <!-- FEEDS -->
        <activity android:name=".Feed.FeedActivity" />
        <activity android:name=".Feed.SearchActivity"
            android:parentActivityName=".Feed.FeedActivity"
            android:theme="@style/Theme.AppCompat.Light"/>
        <activity android:name=".Feed.ViewOutfitPostActivity"/>

        <!-- PROFILE -->
        <activity android:name=".Feed.Profile.Profile" />

        <!-- ACCOUNT SETTINGS -->
        <activity
            android:name=".Feed.Profile.AccountSettingsActivity"
            android:theme="@style/Theme.AppCompat.Light"
            android:parentActivityName=".Feed.Profile.Profile" />
        <activity
            android:name=".Feed.Profile.EditProfileActivity"
            android:theme="@style/Theme.AppCompat.Light"
            android:parentActivityName=".Feed.Profile.Profile" />
        <activity
            android:name=".Feed.Profile.ChangePassword"
            android:theme="@style/Theme.AppCompat.Light"
            android:parentActivityName=".Feed.Profile.AccountSettingsActivity" />
        <activity
            android:name=".Feed.Profile.SignOutActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:parentActivityName=".Feed.Profile.AccountSettingsActivity"
            android:exported="false" />

        <activity android:name=".PhotoProcess.CropActivity"
            android:exported="true"/>
        <activity android:name=".PhotoProcess.ResultActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".PhotoProcess.BaseActivity"
            android:exported="true"/>

        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="@string/file_provider_authorities"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>